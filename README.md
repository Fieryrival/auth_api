# auth_api
here we have segregated the login and general api
use post request on port 4000 and get valid token


then since the auth middleware is implemented on first get function of id.js ....only this would check for the valid credentials and return the required values